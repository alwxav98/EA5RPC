version: '3.8'

services:
  flask-app:
    build: .
    ports:
      - "5000:5000"  # Puerto esté mapeado correctamente
    environment:
      - FLASK_ENV=development
    volumes:
      - .:/app
    restart: always